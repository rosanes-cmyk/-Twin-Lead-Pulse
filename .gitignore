# Secrets & auth — NEVER commit these
credentials.json
token.json
*.credentials.json
service_account*.json

# Browser profiles (hold your logged-in sessions) & scraped data
.rei_profile/
.chat_profile/
*_profile/
chat_dump.txt
chat_raw.txt

# Real data & local config
config.json
leads.txt
*.local.*

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# OS
.DS_Store
